apply plugin: "war"

bootWar {
    mainClassName = "com.okta.developer.product.ProductApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
